//9) VPM = L1 * L2 * L3 Volume de Prisma
algoritmo "exc09L1"

var
c,l,a:real

inicio
    escreval("Informe o comprimento do prisma: ")
    leia(c)
    escreval("Informe a largura do prisma: ")
    leia(l)
    escreval("Informe a altura do prisma: ")
    leia(a)
    escreval("O volume do prisma e: ",c*l*a:10:2)
    

fimalgoritmo
